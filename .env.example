# LLM providers — at least one key needed (unless using the free/Ollama preset)
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
OPENAI_API_KEY=

# Optional: Telegram bot (get from @BotFather)
TELEGRAM_BOT_TOKEN=

# Optional: ElevenLabs TTS (if using elevenlabs backend instead of gemini)
ELEVENLABS_API_KEY=
