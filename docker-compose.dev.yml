services:
  nexus:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./src:/app/src
    environment:
      - PYTHONPATH=/app/src
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
