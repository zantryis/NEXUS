services:
  nexus:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./.env:/app/.env
      - ./data:/app/data
      - ./src:/app/src
    environment:
      - PYTHONPATH=/app/src
      - NEXUS_TRUST_DOCKER_GATEWAY=1
    env_file:
      - .env
    restart: unless-stopped
