services:
  nexus:
    build: .
    ports:
      - "8085:8080"
    volumes:
      - ./data:/app/data
      - ./src:/app/src
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
