services:
  nexus:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./.env:/app/.env
      - ./data:/app/data
    environment:
      - NEXUS_TRUST_DOCKER_GATEWAY=1
    env_file:
      - .env
    restart: unless-stopped
